import schemaForm from './index.vue';
import { withInstall } from '@zdzz/shared';
export const SchemaForm = withInstall(schemaForm);
export declare type SchemaFormInstance = InstanceType<typeof SchemaForm>;
